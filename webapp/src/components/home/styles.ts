import { CSSProperties } from "react";

export const gameButton: CSSProperties = {
  maxWidth: 300,
  marginBottom: 10,
  marginTop: 10,
  height: 100,
  width: "100%",
}

export const buttonStack: CSSProperties = {
  alignItems: "center",
}
