import { CSSProperties } from "react";

export const joinGameStack: CSSProperties = {
  maxWidth: 200,
  alignContent: "center",
  textAlign: "center",
}

export const wrapperStack: CSSProperties = {
  width: "100%",
  alignItems: "center",
}
